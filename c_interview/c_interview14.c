/*
Give a string which contains only lowercase letters, remove duplicate letters so that every letter appear once and only once. You must make sure  your result is the smallest in dictionaryt order among all possible results.

Example:
Given "cbacdcbc"
Step1 1: remove the duplicate letters there are several condidates strings, such as ['cbad', 'bacd', 'badc', 'acdb', 'adbc', ...]
Step 2: remove the duplicated strings acdb is the smallest in dictionary order
Return 'acdb'as the results
*/

string completeString(string str) {
    
}
